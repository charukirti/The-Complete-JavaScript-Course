// Expression = a code block which executes and produces a value.
// Statement = a code block which executes and does not produces a value

// const addition = 2 + 4; // this is an expression
// console.log(addition);

const ageSarah = 18;
if (ageSarah >= 18) {
  console.log("She can drive.");
} else {                           //This whole code is a statement. i.e called as if-else statement
  console.log("She can't drive.");
}

